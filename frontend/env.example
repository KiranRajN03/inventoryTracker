# Local: cp env.example .env.local  →  npm start
# Vercel: set in Project → Settings → Environment Variables (redeploy after change)

# --- Local development ---
REACT_APP_BACKEND_URL=http://127.0.0.1:8000

# --- Vercel production (example) ---
# REACT_APP_BACKEND_URL=https://your-backend-project.vercel.app
